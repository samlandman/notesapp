'use strict';

// class Note {
//   constructor(title){
//     this.title = title;
//
//   }
// };

class NoteApp {
  constructor(){
    this.noteArray = [];
  }

// function(exports) {
   note (title) {
      this.title = title;
      this.noteArray.push(title)
    };

    // Note.prototype.noteArray = [];
  // }
  //(this);
};

// exports.NoteApp = NoteApp;
